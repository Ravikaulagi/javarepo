class user
{  
      
    constructor(names)
    {
        let count=0;
        this.names=names;
        let arr=new Array()
        arr.push(names)
        count++;
        let res=res+count;
        console.log(res)
    }
}
  let res=0
 u1=new user("james");
 u1.res; 
 u1.name